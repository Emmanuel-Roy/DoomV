# arch-test-src is now a proper submodule (see .gitmodules) of
# riscv-non-isa/riscv-arch-test, old-framework-3.x branch. doomv_env/
# (this project's own port of the model_test.h contract) lives as its
# own sibling directory here, not nested inside the submodule.

# Scratch build/run artifacts produced while iterating.
*.elf
*.exe
*.dll
*.sig
__pycache__/
dbgbuild/
